import React, { Component } from 'react';
import './Button.css';

class Button extends Component {
    render () {
        return (
            <div>
            <button className='location'>Store</button>
            </div>
        )
    }
}

export default Button;